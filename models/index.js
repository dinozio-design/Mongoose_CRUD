// Genre is written as a promised base .then .catch
const Genre = require('./Genre');
// Departments is written as an asynchronous function with try catch
const Departments = require('./Departments');

module.exports = { Genre, Departments };